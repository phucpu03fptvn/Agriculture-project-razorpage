@page
@model Ass02Solution_Se1705_NguyenHoangPhuc_SE173197.Pages.AgriculteCategoryManagement.IndexModel

@{
    ViewData["Title"] = "Index";
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Agriculture Category Management Index Page">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/5.3.0/css/bootstrap.min.css">
    <title>Agriculture Category Management</title>
    <style>
        body {
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
        }
        h1 {
            color: #003366;
            margin-top: 20px;
            text-align: center;
        }
        .table {
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        .table thead {
            background-color: #003366;
            color: white;
        }
        .table th, .table td {
            vertical-align: middle;
        }
        .btn-primary {
            background-color: #0056b3;
            border: none;
        }
        .btn-primary:hover {
            background-color: #003d80;
        }
        .action-links a {
            color: #003366;
        }
        .action-links a:hover {
            text-decoration: underline;
        }
        .action-links .btn:hover {
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            transform: translateY(-2px);
        }
    </style>
</head>
<body>
    <div class="container">
        <h1 style="font-weight: bold">AGRICULTURE CATEGORY MANAGEMENT</h1>

        <p class="">
            <a asp-page="Create" class="btn btn-primary">Create New</a>
        </p>

        <table class="table table-striped">
            <thead>
                <tr>
                    <th>
                        @Html.DisplayNameFor(model => model.AgricultureCategory[0].CategoryName)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.AgricultureCategory[0].Description)
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.AgricultureCategory)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(modelItem => item.CategoryName)
                        </td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Description)
                        </td>
                        <td class="action-links text-center">
                            <a asp-page="./Edit" asp-route-id="@item.CategoryId" class="btn btn-primary btn-sm mx-1" style="border-radius: 20px; color: white">
                                Edit
                            </a>
                            <a asp-page="./Details" asp-route-id="@item.CategoryId" class="btn btn-success btn-sm mx-1" style="border-radius: 20px; color: white">
                                Details
                            </a>
                            <a asp-page="./Delete" asp-route-id="@item.CategoryId" class="btn btn-danger btn-sm mx-1" style="border-radius: 20px; color: white" onclick="return confirm('Are you sure you want to delete this item?');">
                                Delete
                            </a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/5.3.0/js/bootstrap.bundle.min.js"></script>
</body>
</html>
